import {AppLayout} from "@/components/app-layout";
import React from "react";


export default function CustomerAccountsLayout({children}: {children: React.ReactNode}) {
    return <AppLayout>{children}</AppLayout>
}